import XCTest
@testable import AnimatedTapBar

final class AnimatedTapBarTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(AnimatedTapBar().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
